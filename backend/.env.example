DATABASE_URI=YOUR_DATABASE_URI_HERE
PAYLOAD_SECRET=YOUR_SECRET_HERE
# URL you use in the browser for this Next/Payload app (must match Origin for admin CSRF + cookies)
PAYLOAD_SERVER_URL=http://localhost:3000
# Optional: separate frontend origin if it calls the API with cookies
FRONTEND_URL=http://localhost:4321
